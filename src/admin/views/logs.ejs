<div class="d-flex justify-content-between align-items-center mb-3">
  <h4 class="mb-0"><i class="bi bi-clock-history"></i> Activity Logs</h4>
</div>

<div class="card">
  <div class="card-body p-0">
    <table class="table table-hover mb-0">
      <thead class="table-light">
        <tr>
          <th>#</th>
          <th>User</th>
          <th>Action</th>
          <th>Admin</th>
          <th>Notes</th>
          <th>Timestamp</th>
        </tr>
      </thead>
      <tbody>
        <% logs.forEach((log, i) => { %>
          <tr>
            <td><small class="text-muted"><%= i + 1 %></small></td>
            <td>
              <strong><%= log.first_name %></strong>
              <% if (log.username) { %><small>@<%= log.username %></small><% } %>
            </td>
            <td>
              <% if (log.action === 'verified') { %>
                <span class="badge bg-success">✅ Approved</span>
              <% } else if (log.action === 'rejected') { %>
                <span class="badge bg-danger">❌ Rejected</span>
              <% } else if (log.action === 'requested') { %>
                <span class="badge bg-info">📝 Registered</span>
              <% } else if (log.action === 'banned') { %>
                <span class="badge bg-dark">🚫 Banned</span>
              <% } else { %>
                <span class="badge bg-secondary"><%= log.action %></span>
              <% } %>
            </td>
            <td><code><%= log.performed_by %></code></td>
            <td><small><%= log.notes || '—' %></small></td>
            <td><small class="text-muted"><%= new Date(log.created_at).toLocaleString() %></small></td>
          </tr>
        <% }) %>
        <% if (logs.length === 0) { %>
          <tr><td colspan="6" class="text-center text-muted py-4">No activity recorded</td></tr>
        <% } %>
      </tbody>
    </table>
  </div>
</div>